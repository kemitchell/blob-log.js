var BlobLog = require('./')
var tape = require('tape')

tape('construction', function (test) {
  
})

tape('construction without new', function (test) {
  
})

tape('regular-mode write stream', function (test) {
  
})

tape('object-mode write stream', function (test) {
  
})

tape('read stream', function (test) {
  
})

tape('read stream from index', function (test) {
  
})
